You are reviewing a CSV export of WeChat group messages. The CSV has two columns: `sender_nickname` and `content`. You must append a new column named `information_weight` with a numeric value between 0.0 and 1.0 (inclusive) for each row to capture how information-dense the message is.

Target CSV file: $TARGET_FILE

Workflow (run each step explicitly):
1. Read the entire file once to understand its structure and themes. Do not make edits yet.
2. Revisit the rows in manageable batches (no more than ~300 records per editing pass). After each batch, insert an `information_weight` column entry, save the changes, then continue with the next batch until every row is scored. Add at most two decimal places. Use consistent judgment, reading each row carefully instead of relying on regular expressions or bulk search-and-replace.
3. Reread the final CSV to ensure it is valid (same number of commas per row as the header, weights present on every row, UTF-8 text intact). Abort if you notice corruption.

Scoring guidance (examples, not exhaustive):
- 1.0 — novel insights, technical fixes, decisions, or multi-sentence explanations that could influence later analysis.
- 0.75 — focused follow-ups, concrete questions, or references to actionable resources.
- 0.5 — conversational check-ins or short comments that relate to ongoing discussions but add limited new information.
- 0.25 — acknowledgements, brief reactions, or messages with little informational value.
- 0.0 — pure emoji, stickers, spam, or administrative boilerplate.

Rules:
- Do not delete or reorder rows. Preserve the header as the first line.
- If the CSV already contains an `information_weight` column with non-empty values, stop immediately and report that the file appears processed.
- Avoid creating helper files or using code to post-process the CSV; apply edits directly within the file.
- Keep all text in UTF-8. Escape quotes exactly as needed for CSV.
- Respond with a short status summary after finishing or skipping the file.
- If you notice partial progress from an earlier run (some weights populated, others blank), continue the batching process only on the blank rows until the file is complete.
